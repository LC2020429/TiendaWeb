'use strict'
import category from "./category.model.js";

// Metodos para categoria
// Añadir categoria a producto
export const addCategory = async (req, res)=>{
    try{
    // necesitamos primero los productos
    }catch(err){

    }
}